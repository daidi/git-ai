{
  "name": "git-ai-update-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for git-ai CLI updates",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
